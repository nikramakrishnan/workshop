#include<stdio.h>

int main()
{

    int x = 22;
    int y = 78;

    printf("x is %d\n", x);
    printf("y is %d\n", y);

    void add(int a, int b){
        int sum = a+b;
        print (sum)
    }

    //swapping code...
    int temp = x;
    x = y;
    y = temp;

    printf("x is now %d\n", x);
    printf("y is now %d\n", y);

}